
export interface ICurvePriceConfig {
  maxPrice : bigint;
  minPrice : bigint;
  maxLength : bigint;
  baseLength : bigint;
  precisionMultiplier : bigint;
  feePercentage : bigint;
  isSet : boolean;
}
